//Operators
// JavaScript supports several operators for performing arithmetic, comparison, and logical operations. Some common ones include:

Arithmetic operators: +, -, *, /, %
Comparison operators: ==, !=, >, <, >=, <=
Logical operators: && (AND), || (OR), ! (NOT)//


//Arithmetic Operators
Arithmetic operators are used to perform mathematical calculations://
//
let x = 5;
let y = 3;

console.log(x + y); // 8
console.log(x - y); // 2
console.log(x * y); // 15
console.log(x / y); // 1.6666666666666667
console.log(x % y); // 2

Logical Operators
Logical operators are used to combine and manipulate boolean values:

javascript
Copy code
let x = 5;
let y = 3;

console.log(x > 3 && y < 5); // true
console.log(x > 3 || y > 5); // true
console.log(!(x > 3)); // false

Assignment Operators
Assignment operators are used to assign values to variables:

let x = 5;

x += 3; // equivalent to x = x + 3
console.log(x); // 8

x -= 3; // equivalent to x = x - 3
console.log(x); // 5

x *= 3; // equivalent to x = x * 3
console.log(x); // 15

x /= 3; // equivalent to x = x / 3
console.log(x); // 5

x %= 3; // equivalent to x = x % 3
console.log(x); // 2


These are just a few examples of operators in JavaScript. There are many more operators and they can be used in different ways to achieve different results.


