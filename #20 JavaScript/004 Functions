////Functions
//Functions are reusable blocks of code that perform a specific task. 
//They can take parameters as input and return a value as output. Here's an example://
function addNumbers(num1, num2) {
    return num1 + num2;
  }
  
  let sum = addNumbers(3, 5); // sum = 8
  